TODO:

Tests:
    search dropdown
        sic collection, exhaustion - on homepage, on airport lookup, flight lookup and terminal lookup.
        test for raw query top item exact digit for flight number match selection
    
# TODO: make .env a standard file creation during setup phase and initiate with variable production vs dev.
    # initiating with dev should reflect dev setup variables.

